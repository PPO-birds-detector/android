<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:popupMenuStyle">@style/SelectDetectorMenuTheme</item>
    </style>

    <style name="SelectDetectorMenuTheme" parent="@android:style/Widget.DeviceDefault.ListPopupWindow">
        <item name="android:popupBackground">@color/background_material_light</item>
    </style>
</resources>
